[phases.setup]
nixPkgs = ["nodejs_20", "yt-dlp", "ffmpeg"]

[phases.install]
cmds = ["npm install --no-audit --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"
